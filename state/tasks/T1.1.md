# T1.1 Bench2Drive + SimLingo 评测 harness 装配

- status: done(2026-08-28 验收 PASS:3 路线 RC=100 DS=100,server 0 崩)
- gpu_class: D(装配);空场景冒烟时短时 A
- 输入:SimLingo 仓锁 commit;Bench2Drive 钉 v0.0.3;UE4 server 2031
- 输出:评测入口脚本;PythonAPI 拷入 WSL ext4 + PYTHONPATH;3 条路线 leaderboard 空场景冒烟通过(T3.0 前置)
- 时长 0.5–1 天

## progress(2026-08-28 prep-agent)

### 仓钉死

- `external/bench2drive` 已 clone(144MB)。**远端无任何 tag**——0.0.3 是分支(`refs/heads/0.0.3`)。已 checkout 其 tip:**commit `2645714eb1f3a100217928dd113093cae0779f36`**(2026-05-23, "Update README.md"),detached HEAD。master tip(0.0.4)= `7ec25d1`,勿混用。

### leaderboard 结构笔记(接线靠此节,不用重摸)

- 评测入口:`external/bench2drive/leaderboard/leaderboard/leaderboard_evaluator.py`。关键参数:`--host`(默认 localhost,WSL→Windows 用 `ip route show | awk '/default/{print $3}'` 动态取)、`--port`(默认 2000,**我们传 2031**)、`--traffic-manager-port`(默认 8000)、`--routes`、`--agent`、`--agent-config`、`--checkpoint`(结果 json)、`--debug-checkpoint`、`--gpu-rank`、`--resume`。
- 官方参考脚本:`leaderboard/run_leaderboard.sh`(demo)与 `leaderboard/scripts/run_evaluation.sh`(实战,被 `run_evaluation_multi_*.sh` 调)。后者 PYTHONPATH 范式:`${CARLA_ROOT}/PythonAPI` + `${CARLA_ROOT}/PythonAPI/carla` + linux egg(**我们没有 linux egg,删掉该行,pip carla==0.9.15 顶替**)+ `leaderboard` + `scenario_runner`;外加 `LEADERBOARD_ROOT` / `SCENARIO_RUNNER_ROOT` / `CARLA_ROOT` 环境变量。
- 220 路线:`leaderboard/data/bench2drive220.xml`,220 条全部**内联** `<scenarios>`+`<weathers>`,无外部 scenario json 依赖。44 种 scenario type × 5 条,type 名与仓内 `scenario_runner/srunner/scenarios/` 一一对应(已核对全覆盖)。town 分布:Town12×104、Town13×47、Town04×12、Town03×11、Town05×9、Town11×7、Town15×7、Town06×6、Town07×5、Town01×4、Town02×4、Town10HD×4。
- 并行套路:`tools/split_xml.py <base无后缀> <N> <algo> <planner>` 把 220 xml 切成 N 份,每份独立 `--port`/`--traffic-manager-port` 各起一个评测进程(见 run_evaluation_multi_tcp.sh);`tools/merge_route_json.py <folder>` 合并各 split 的 checkpoint json,输出 driving score(mean score_composed)/ success rate(completed 且无违章,min_speed 豁免)。
- 结果:statistics_manager 每条路线写 `score_route`(RC%)/`score_penalty`/`score_composed`(DS=RC×penalty),checkpoint json 的 `_checkpoint.records` 数组,支持 `--resume` 断点续跑。
- agent 接口:继承 `leaderboard/autoagents/autonomous_agent.py`,实现 `setup(path_to_conf)` / `sensors()` / `run_step(input_data, timestamp)`;参考 `npc_agent.py`(用 `agents.navigation.basic_agent`)与 `dummy_agent.py`。
- `agents` 模块是硬依赖:route_parser/route_manipulation/npc_agent/多个 scenario 都 import `agents.navigation.*`,来自 CARLA PythonAPI(落位见下)。
- scenario_runner 用仓内自带版(`external/bench2drive/scenario_runner`,CARLA_VER 写 0.9.13 但与 0.9.15 配套使用无碍),**不要**另装 pip scenario_runner。
- 其他 data:`routes_devtest.xml` 2 条、`routes_validation.xml` 20 条、`routes_training.xml` 90 条、`drivetransformer_bench2drive_dev10.xml` 10 条;`weather.xml` 是采数目录,与 220 评测无关。

### 220 路线资产完整性

- 路线/场景/天气:全在仓内(内联),220 条 id 唯一(1711–28330),✅。
- 地图:Windows 侧 0.9.15 已含全部 12 个所需 town——Town01–07+_Opt、Town10HD+_Opt 单文件 map;Town11/12/13/15 为 LargeMap 目录(Town12 已见 Town12.umap + 36 tile),OpenDrive xodr 齐(Town12/15 的 xodr 在各自子目录)。**AdditionalMaps 已装,无需补包**,✅。

### 冒烟路线(已落 `leaderboard/data/bench2drive_smoke3.xml`,3 条子集)

- **25975 Town07,49m/17wps**:全部 220 条中最短;场景 VanillaSignalizedTurnEncounterGreenLight(绿灯路口直行/转向,简单);白天多云(cloud 100)。
- **26401 Town06,124m/27wps**:Town06 中最短;场景 MergerIntoSlowTrafficV2(汇流);**首段 sun_altitude=-90 夜景**——顺带覆盖夜间渲染,若嫌难可换 26394(137m)。
- **28154 Town10HD,155m/28wps**:场景 T_Junction;白天清晨(sun_alt 15)。xml town 属性写的是 `Town10HD`;要更快/更省显存可 sed 成 `Town10HD_Opt`(坐标系一致,0.9.15 两图同路网,项目惯例用 _Opt)。
- 选路依据:keypoint 折线长度排序取各镇最短;三条分属三个 town 三种场景,覆盖昼夜。

### PythonAPI 落位

- 已拷 `/mnt/c/carla/CARLA_0.9.15/WindowsNoEditor/PythonAPI` → **`/home/xsl/carla0915-pythonapi`**(26MB,55 文件,`diff -r` 与源全同)。
- 结构:`carla/agents/{navigation,tools}`、`carla/dist`(仅 win wheel/egg,WSL 不可用)、`util/`、`util/opendrive`、`examples/`。**shipped 包里无 util/dependency 子目录**(源码头绪,发行包没有)。
- 接线 PYTHONPATH=`/home/xsl/carla0915-pythonapi:/home/xsl/carla0915-pythonapi/carla:external/bench2drive/leaderboard:external/bench2drive/scenario_runner`(`import carla` 由 pip carla==0.9.15 提供;已实测 `agents.navigation.global_route_planner` 与 `leaderboard.utils.route_parser` 在该 PYTHONPATH 下 import OK)。

### 依赖预检(mysim-simlingo,**只列未装**)

必装(harness 核心路径硬依赖,env 现缺):
- `dictor`、`requests`、`tabulate`、`simple-watchdog-timer`、`ephem`
- `py-trees==0.8.3`(钉死,srunner 0.9.15 用 py_trees 0.8 API,2.x 不兼容)
- `shapely==2.0.0`(官方 pin 1.7.1 无 cp310 wheel;2.0.0+numpy1.26.4 为 mysim-ue5 已验证组合;srunner 只用 geometry/affinity 基础 API)

已满足,勿按官方 pin 降级:
- `numpy==1.26.4`(官方 pin 1.18.4 弃)、`networkx==3.4.2`(官方 pin 2.2 弃,已核查无 `.node[` 等 2.x 遗留 API)、`carla==0.9.15`

可选/不装:
- `opencv-python(-headless)==4.10.0.84`:cv2 仅 `actorcontrols/visualizer.py` 顶层 import,该模块 importlib 惰性加载,220 路线无 SimpleVehicleControl 不触发;**SimLingo agent 自身大概率要 cv2,归 T1.0 装**(注意两 agent 协调版本)
- 不装:`pygame`(human_agent)、`transforms3d`/`pexpect`(ROS agent)、`xmlschema`(OpenSCENARIO 专用)、`matplotlib`(metrics 示例)、`psutil`/`distro`/`six`(核心路径无 import)
- leaderboard 官方 `opencv-python==4.2.0.32` pin 无 cp310 wheel,弃

### 已打兼容补丁(external/bench2drive 内,git diff 可查)

- `leaderboard/leaderboard/leaderboard_evaluator.py`:`import pkg_resources` → `importlib.metadata`(2 处,版本检查改用 `importlib.metadata.version("carla")`)。原因:**setuptools 83 已移除 pkg_resources**,原代码在 mysim-simlingo 直接 ImportError。已 `py_compile` 通过。`scenario_runner/scenario_runner.py` 同款用法自带 importlib.metadata 优先路径,未动。

### 坑

- **Bench2Drive 仓无 git tag**,0.0.3/0.0.4 都是分支;钉 0.0.3 要用 commit `2645714e`(本卡已落 detached HEAD)。
- **setuptools≥81 移除 pkg_resources** 直接卡死 leaderboard_evaluator import——要么打上述补丁(已打),要么 env 里 `setuptools<81`(会与并行移植冲突,不推荐)。
- 官方 run_evaluation.sh 的 linux egg 路径在我们(Windows server 包)不存在,自写运行脚本时删掉该行,`import carla` 走 pip 包。
- Windows 发行包 PythonAPI 无 `util/dependency` 子目录(任务书提及,实测不存在,不影响 harness)。
- 220 xml 的 Town10HD 指全量 HD 图;冒烟想快用 Town10HD_Opt,直接改 route town 属性即可(坐标兼容)。

### 待办(T1.0 完成后,接线 agent)

1. 协调装"必装"7 包(TUNA 源);装完用 `python leaderboard_evaluator.py --help` 验证 import 链。
2. 自写运行脚本(参照 run_evaluation.sh):host 动态取、port 2031、TM 8000、PYTHONPATH 如上;`LEADERBOARD_ROOT=external/bench2drive/leaderboard`、`SCENARIO_RUNNER_ROOT=external/bench2drive/scenario_runner`。
3. agent 先用 `autoagents/npc_agent.py` 或 dummy 顶,跑 `bench2drive_smoke3.xml` 空场景冒烟(如需纯空场景,把各 route 的 `<scenarios>` 块清空即可)。

## progress(2026-08-28 接线 agent)

### 接线方式(最终形态)

- 评测栈:`external/bench2drive`(0.0.3 @2645714)leaderboard + scenario_runner + SimLingo 官方 agent `external/simlingo/team_code/agent_simlingo.py`(`LingoAgent`),env `mysim-simlingo`(py3.10 + carla==0.9.15 pip 包顶替 linux egg)。
- `--agent-config` 传 ckpt 文件路径;evaluator 内部自动拼 `+<save_name>`,agent `setup()` 按 `+` 劈开:`config_path` = ckpt,`save_path_root` = save_name。
- **ckpt 布局适配**(agent 硬编码 `Path(ckpt).parent.parent.parent/.hydra/config.yaml`):新建 `data/checkpoints/simlingo_eval/`——`checkpoints/epoch=013.ckpt/pytorch_model.pt` 是指向 `data/checkpoints/simlingo/pytorch_model.pt` 的符号链接,`.hydra/config.yaml` 是 T1.0 `hydra_config.yaml` 的拷贝。
- InternVL2 conversation.py:agent 用 `hydra to_absolute_path('pretrained/InternVL2-1B')`(未初始化 hydra 时按 cwd 解析);evaluator 又必须 `cd external/bench2drive`(`get_weather_id` 读相对路径 `leaderboard/data/weather.xml`)——故把 T1.0 冒烟留下的 `logs/t10-smoke/pretrained/InternVL2-1B/` 拷到 `external/bench2drive/pretrained/InternVL2-1B/`。
- PYTHONPATH(实测 18/18 import OK,tools/t11_import_test.py):`/home/xsl/carla0915-pythonapi` + `…/carla` + `bench2drive/leaderboard` + `bench2drive/scenario_runner` + `simlingo` + `simlingo/team_code`(agent 里 `import scenario_logger` 是顶层 import,必须带 team_code 目录)。
- 必设 env:`SAVE_PATH`(结尾带 '/',agent 字符串拼接;同时是 agent_wrapper 的 Bench2Drive 模式开关)、`ROUTES`、`HF_ENDPOINT=https://hf-mirror.com`、`WANDB_MODE=offline`、`B2D_EXTERNAL_SERVER=1`(见补丁)。

### 依赖(在 prep 清单之上补)

- 已装清单内 5 包:dictor / tabulate / simple-watchdog-timer / ephem / py-trees==0.8.3(requests、shapely 已在;**shapely 保持 T1.0 的 1.8.5.post1 不换 2.0.0**——srunner 只用 geometry/affinity 基础 API,1.8.5.post1+numpy1.26.4 实测 import OK)。
- **清单漏了 3 个硬 import 依赖**:`pexpect` + `transforms3d`(agent_wrapper 无条件 import ros_base_agent 连带)+ `rdp`(simlingo scenario_logger 顶层 import)。已补装(TUNA)。

### 兼容补丁(本轮新增,均可 git diff 查)

- `external/bench2drive/leaderboard/leaderboard/leaderboard_evaluator.py`:`_setup_simulation` 原逻辑在 WSL 起本地 `CarlaUE4.sh`(不存在)并 `find_free_port` 改端口——加 `B2D_EXTERNAL_SERVER=1` 分支:跳过本地启动、端口保持传入值、跳过 30s sleep。
- `route_parser.py`(leaderboard/utils)+ `route_parser.py`/`scenario_parser.py`(scenario_runner/srunner/tools):3 处 `Element.getchildren()`(py3.9 移除)→ `list(scenario)`。
- `external/simlingo/team_code/transfuser_utils.py` `PIDController.step`:`error` 是 shape-(1,) 数组,与 deque 初始 int 0 混合,`np.mean(window)` 在 numpy≥1.24 抛 inhomogeneous shape——`float(error)` 统一(数值语义不变)。

### 运行脚本

- `tools/run_eval_ue4.sh [routes_xml] [out_dir] [port] [max_restarts]`(默认 smoke3 / logs/t11-smoke3 / 2031 / 3):ensure server →(必要时)起评测 → 非零退出则 ensure(杀+adapter 轮询+显存校验)+ `--resume=True` 续跑,最多 max_restarts 次。
- `tools/t11_ensure_server.py`:复用 server_watchdog 的 SIDES/kill_side/launch_once/restart;额外加"RPC 通但 5090 显存 <5000MiB 判错卡→重启"。
- **首轮运行绝不能传 `--resume=False`**:evaluator `--resume` 是 `type=bool`,`bool("False")=True`——首轮省略该参数,续跑才 `--resume=True`。

### 冒烟记录(2026-08-28,smoke3,Town10HD 已 sed 成 Town10HD_Opt)

**PASS**:3/3 路线 Completed,单次 attempt 跑完(无 server 崩溃、无 resume),总耗时 1816s(~30min)。产物 `logs/t11-smoke3/`(result.json + eval_attempt0.log + viz/,共 1.8MB)。

| route | town | 场景 | 长度 | RC | penalty | DS | game 时 | wall 时 |
|---|---|---|---|---|---|---|---|---|
| 25975 | Town07 | VanillaSignalizedTurnEncounterGreenLight(白天多云) | 52m | 100 | 1.00 | 100 | 50.7s | 809s |
| 26401 | Town06 | MergerIntoSlowTrafficV2(夜景 sun_alt=-90) | 125m | 100 | 1.00 | 100 | 11.6s | 215s |
| 28154 | Town10HD_Opt | T_Junction(清晨) | 154m | 100 | 1.00 | 100 | 30.9s | 734s |

- 全局:entry_status=Finished,eligible=True;碰撞/红灯/stop/越线/堵死全 0;仅 min_speed_infractions(周边无车流时该判据误报,Bench2Drive merge_route_json 本来就豁免 min_speed)。
- 推理速率 ~0.04–0.065× 实时(每帧 ViT 2×448² + ≤100 token 生成,~0.7–1s/帧;夜路 Town06 反而快因为场景短直)。
- 显存:server ~8.7GB + agent 推理峰值总量 ~10.4GB,32GB 余量充足。
- server 全程稳定:0 次 Watchdog exception / Engine crash / Simulation crashed。

### 坑(本轮新踩)

- **evaluator `_setup_simulation` 会在 WSL 本地起 `CarlaUE4.sh`**(路径不存在)且 `find_free_port` 可能改掉传入端口——已加 `B2D_EXTERNAL_SERVER=1` 补丁跳过;运行脚本默认 export。
- **3 处 `Element.getchildren()`**(py3.9 移除)卡 route 解析:bench2drive leaderboard/utils/route_parser.py + srunner/tools/{route_parser,scenario_parser}.py → `list(scenario)`。
- **PIDController 在 numpy≥1.24 必崩**(deque 初始 int 0 与 shape-(1,) 数组混合,`np.mean` 抛 inhomogeneous shape)——`team_code/transfuser_utils.py` step 内 `float(error)`。官方跑的是老 numpy 才没暴露。
- prep 依赖清单漏:`pexpect`、`transforms3d`(ros_base_agent 被 agent_wrapper 无条件 import)、`rdp`(scenario_logger)。
- 官方代码无害 bug(未修,不影响分数):agent `destroy()` 读 `cfg.data_module.encoder`(release 配置无此键)抛 ConfigAttributeError,被 evaluator catch,只打印 "Failed to stop the agent";`del model` 在异常前已执行,无显存泄漏。
- evaluator 每条路线把 `+<save_name>` 追加到 agent_config 且不去重,agent 取 `split('+')[1]` —— 单进程多路线时 route 2/3 的 viz 日志都落到 route 1 的目录名下(官方 SLURM 一路线一进程所以无感)。冒烟无碍,正式跑 220 若在意按路线拆分 viz 需另处理。
- route 26401 有一条 `Cannot spawn actor vehicle.*` 警告(背景车流某个出生点被占),非致命。
- UE4 server 冒烟后仍在跑(adapter=0, 2031):停 = `powershell.exe Stop-Process -Name CarlaUE4-Win64-Shipping,CarlaUE4 -Force`。

# T1.2 SimLingo 官方权重 Bench2Drive 220 评测(sanity check)

- status: running(eval-agent)
- gpu_class: C(GPU 长任务,锁独占;UE4 server 2031 已在跑,adapter=0)
- 输入:ckpt `data/checkpoints/simlingo/`(HF RenzKa/simlingo epoch=013,commit 26c7c89e);Bench2Drive 0.0.3@2645714;harness `tools/run_eval_ue4.sh`(T1.1 已验)
- 流程:**先 20 路线冒烟(量实测墙钟/路线,外推 220)** → 全量 220(`split_xml.py` 切分可并行?单机单卡只能串行)
- 判据:DS 85.07±0.95 / SR 67.27±2.11(论文 Table 2,3 seeds),±3 DS 内或书面解释;只作 sanity 不进 M5 对比表
- 风险:T1.1 实测推理 0.04–0.065× 实时,220 全量墙钟可能远超 8–18h 估计——20 路线冒烟后必须先出外推报告再决定全量
- 输出:`EXP-…` DS/SR 明细(logs/t12-*/),显存同存实测回填

## 执行日志
- 2026-08-28:阶段 A 启动。官方 split_xml.py 仅顺序等分,无分层范式 → 自建 `tools/t12_select_split20.py`(12 镇全覆盖,配额偏 Town12/13:LargeMap 占全量 75%,外推精度靠它;镇内等间距抽,保持官方相对顺序)→ `external/bench2drive/leaderboard/data/bench2drive_split20.xml`(20 条:01/02/05/06/07/10HD/11/15 各1,03/04 各2,12×5,13×3)。
- harness 一处修正:run_eval_ue4.sh 续跑判定原要求 attempt>0,脚本整体退出后重跑同命令会从零覆盖 result.json(与头注矛盾);改为仅靠 result.json 存在性判断(首轮无文件自然不传 --resume)。
- 后台任务 bash-0ptk3mge:`tools/run_eval_ue4.sh .../bench2drive_split20.xml logs/t12-split20 2031 10`。启动前 server 在跑(2031 开,显存 10363MiB,宿主 free ~39GiB)。

## 阶段 A 结果(2026-08-28,已完成,evaluator rc=0,零重启)
- 20/20 完成,总墙钟 23159s(6.43h),总 game 955s,综合 ~24.3× 慢于 game 时间(推理 ~0.041–0.05× 实时)。
- **分数:mean DS 92.54,Completed 19/20**;17 条 DS=100 零违章。异常 3 条:
  - 25845 Town15 AccidentTwoWays:**Failed-TickRuntime**(route timeout 200s game=0.4s/m×~500m),爬车撞上事故车,DS=25.2/RC=41.9,单条 wall 4873s(81min);
  - 28330 Town12 SignalizedJunctionLeftTurnEnterFlow:RC=100 但 2 车撞+1 越线,DS=32.7,wall 4413s;
  - 25968 Town07:1 次越线,DS=92.8。
- **外推 220 全量墙钟:~63h(分层 town 均值 63.7h;长度归一回归 wall=−19+23.8×game 62.7h;朴素均值×220 68.3h)——超 36h 阈值,按任务规则不进阶段 B**。主因 Town12 104 条×~1100s ≈ 31–33h;长尾来自场景动力学(车流汇入/事故绕行爬车),非抽样偏差(样本 polyline 与全量逐镇吻合)。
- 资源实测(运行中间歇采样):5090 显存总量 10.4–14.0GB(server ~8.1 + agent ~2–6GB),宿主 free RAM 18–39GiB,全程无 OOM/无 server 崩溃。
- 工具:新增 `tools/t12_select_split20.py`(分层抽样)、`tools/t12_progress.py`(进度+外推统计)。
- 下一步(CP1 决策输入):若要对齐官方 85.07 须跑全量,选项:①接受 ~63h 串行(单机单卡无并行空间);②砍 SECONDS_GIVEN_PER_METERS 压长尾(改变 benchmark 语义,破坏与官方可比性);③换子集评测协议(如 dev10/官方 mini)作 sanity 替代。

## 阶段 B(2026-08-28,执行中)
- **补丁(external/simlingo,git diff 可查)**:
  - `simlingo_training/models/language_model/llm.py`:①A3 `LLM.forward` 改走 `backbone_module()`(逐层剥 PeftModelForCausalLM→Qwen2ForCausalLM→Qwen2Model 外壳;LoRA 注入在层内,数学等价已实测 features diff=0),只取 `last_hidden_state`,全序列 logits 死代码移除(省 ~3.3GB/帧显存 churn);forward 增加 `past_key_values` 形参供 cache 追加前向。②B1 `greedy_sample` kv-cache 化(prefill 一次 + 逐 token 缓存解码;采样逻辑/temperature/top_k/top_p/eos 早停与 eos-embedding 追加语义不变),返回 `(sampled_tokens, input_embeds, past_key_values, pending_embeds)`;原实现原样保留为 `greedy_sample_legacy`。
  - `simlingo_training/models/driving.py`:`eval_fastpath` 开关(env `SIMLINGO_FASTPATH`,默认 1);fast 路径 driving query 复用生成 cache 追加前向(pending_embeds 拼最前,切 `[-len_driving:]`,**修正了 t12b_kvcache_ab.py 的 off-by-one 切片**);legacy 路径走 `greedy_sample_legacy` + 整序列重算(与官方数值 bit 等价)。
  - `team_code/agent_simlingo.py`:A1 metric dump 降频 `step % 50` + destroy() 补写最终版;A2 conversation.py 模块 setup 时缓存(消每帧 exec_module);B2 eval-only LoRA `merge_and_unload()`(仅 fastpath,训练不动);D destroy() `getattr(self.cfg.data_module,'encoder',None)` 兜底(原每条路线结尾抛 ConfigAttributeError)。
  - `tools/run_eval_ue4.sh`:export `SIMLINGO_FASTPATH=${SIMLINGO_FASTPATH:-1}`。
- **离线数值验证**(`tools/t12b_validate_patched.py`,产物 `state/tasks/T1.2b-validate-patched.json`,8 帧假数据):
  - B1 隔离(同权重 kv-cache vs 原路径):max d_wps=0.125m / d_route=0.125m,7/8 文本一致(1 帧 29-token 长生成数字位翻转 38.3→38.4)——**比 profile 报告的 0.25–0.9m 小一个量级**(profile 脚本 driving features 切片有 off-by-one,把 eos 位置算了进去,本实现已修正)。
  - B2 隔离(merge 前后同 legacy 路径):max d_wps=1.5m / d_route=0.25m(bf16 权重合并舍入,经 cumsum 放大,主要在随机噪声输入的任意文本帧)。
  - B1+B2 合计 vs 原路径:max d_wps=1.5m / d_route=0.25m;6/8 文本一致。假数据为随机噪声,文本分布在垃圾输入上本就任意,闭环 A/B 才是真判据。
  - 速度:模型段中位 1555ms→543ms,**离线 2.86×**(B1 单独 1.76×,merge 再叠加)。
- **闭环 A/B**(3 路线:24841 Town01 长路线 + 27515 Town03 + 25928 Town11;xml `bench2drive_ab3.xml`;判据:每路线 DS 差 ≤2 且无新 infraction 类型):进行中。

## 阶段 A 结果(2026-08-28,orchestrator 代记)
- 20 路线(split:12 镇全覆盖,LargeMap 占 10 条)全跑完,19 Completed + 1 Failed(RouteScenario_25845 TickRuntime,sys 4873s 单条最长)
- 子集聚合:DS(score_composed)=92.54 / RC=97.10 / IP=0.943(注:子集偏易且含 1 条失败路线,不代表 220 水平)
- 墙钟:Σ system 6.2h / 实际 ~6.5h(09:55–16:22)。**外推 220 全量 ~70–100h(LargeMap 占 75%,大图单条 35–80min)> 36h 阈值**
- 结论:暂不启动阶段 B;先做推理瓶颈 profiling(找非官方偏差型提速空间),再定全量策略

## 阶段 B 结果(2026-08-29,T1.2 收口:**PASS**)

- **220 全量双实例并发跑完**(halfA 113 条 ue4/2031 + halfB 107 条 ue4b/2041,`tools/t12_split220_dual.py` town 内轮流发牌对半分;FASTPATH=1+MERGE=1):
  - **DS=86.18 vs 官方 85.07±0.95 ✓(差 1.11,判据 ±3 内)**
  - **SR=68.2%(150/220 条 DS=100 零违章)vs 官方 67.27±2.11 ✓**(SR 口径按满分路线占比,数值吻合佐证;Completed 状态 207/220=94.1% 是另一口径)
  - RC=96.94 / pen=0.886;13 条非 Completed:9 TickRuntime(卡死耗尽路线预算)+2 blocked+2 deviated,清单 `logs/t12-220-merged.json`
  - 明细:halfA `logs/t12-220-halfA/result.json`(DS=89.04 含重跑)、halfB `logs/t12-220-halfB/result.json`(DS=84.20);viz 两侧各自 viz/ 下
  - 墙钟:两侧并行净耗时 ~22.5h(23:17–次日 21:57,含事故停损 ~7h);Σ route system 34.5h——单实例串行外推 ~55–70h,并发+fastpath 合计提速 ~2.5–3×
- **DS 闸(split20,采纳 FASTPATH 的判据)**:fast DS=94.39 vs orig 92.54(噪声带内不降),wall 9965s vs 22363s=2.24×;ab3 三臂行为扰动呈混沌(见坑录)
- **事故与自愈**(全录 AGENTS.md 坑录 T1.2 全量条目):evaluator 超时后清理段死锁×3(guardian 兜底)+ 空转 server 烧 GPU×1 + 误杀 server×1,断点续跑零进度损失
- **对 M5 的约束**:本卡只作 sanity 不进对比表(判据原文);fastpath 数值与官方有 bf16 级差异(§T1.2b),对外引绝对 DS 须声明 eval 路径;UE4/UE5 两侧用同一 eval 路径,对比可控
- 遗留:13 条失败路线归因分析(TickRuntime 卡死为主,与 25845 同类)归 CP1 汇总材料;AutoMoT/MindDrive 侧 220 复跑时直接复用双实例基建

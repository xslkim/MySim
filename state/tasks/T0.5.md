# T0.5 UE5(0.10)冒烟 + API 探测 + 资源门禁实测

- status: done(2026-08-28 验收 PASS;端口修订 2000→2021 待 CP0 用户拍板)
- progress: |
    【T0.5 执行完毕,2026-08-28 02:40,结果 PASS(带 1 个重大环境坑+1 个端口决策)】
    1) 启动崩溃根因(6 连崩,0xe06d7363/GameThread/~60s):PDB 符号化崩溃栈(exe+RVA,工具 tools/t05_symbolize.py)+
       minidump ThrowInfo RTTI 取证 → 抛出 std::system_error,栈为
       `FCarlaServer::Start → FCarlaServer::FPimpl ctor → rpc::server ctor → asio acceptor bind → throw`。
       即 **RPC 端口 bind 失败**。实测:TCP 2000 落在 Windows 保留段 `1921–2020`(winNAT/Hyper-V 动态保留,
       `netsh int ipv4 show excludedportrange tcp`),PowerShell bind 实测 AccessDenied;2021 BIND_OK。
       **解决:换 -carla-rpc-port=2021 后 6s 就绪**。⚠️ UE4 侧约定端口 2010–2012 同样全在该保留段,T0.6 必撞,
       建议修订端口约定为 2021/2022/2023(UE5)与 2031/2032/2033(UE4)或由管理员释放保留段后复查。
    2) adapter 结论:-graphicsadapter=0 与 =2 的崩溃 XML 均显示 RHI.AdapterName=NVIDIA GeForce RTX 5090
       (UE 枚举 DXGI 物理适配器,IddCx 虚拟显示卡不参与计数),正式跑用 adapter=2;nvidia-smi 进程列表可见
       CarlaUnreal-Win64-Shipping.exe(PID 27260)驻留 GPU。UE5 Shipping 默认不落 CarlaUnreal.log(-log 也不落),
       Saved 目录在 %LOCALAPPDATA%\CarlaUnreal\Saved(不在游戏目录)——AGENTS.md 预备坑"读 UE 日志坐实 adapter"
       在此机上不可用,改用崩溃 XML/nvidia-smi 取证。
    3) probe(logs/probe-ue5.json):14 传感器 blueprint 全 ok(含 depth/semantic/instance/DVS/optical_flow/双 LiDAR);
       weather 读写、交通灯×15、TM、sync(0.05)、spectator 全 ok;Town10HD_Opt spawn 点 155;**无缺失项**。
    4) smoke 5min(logs/smoke-ue5.json)PASS:RPC 延迟 43ms;同步 200tick/4.73s=**42.3 FPS**(目标 20);
       RGB 1280×720 **12317 帧/304.6s≈40.4FPS,0 丢帧**(raw 43.3GB);TM 在 8000 可用;
       available_maps 仅 Mine_01 + Town10HD_Opt 两张;无 vehicle.tesla.model3(回退 taxi.ford)。
       已知客户端坑:carla-ue5-api 0.10.0 结束时恢复异步 apply_settings 触发 C++ abort(客户端 SIGABRT exit134,
       server 无感),tools/smoke_carla.py 已改为先落盘 JSON 再恢复异步。
    5) RAM 门禁(56GB 口径,server 运行中):基线 host free ~23GiB;WSL 压 30GB(tools/t05_ram_gate.py,
       stress-ng 无+sudo 要密码→python 兜底)**host free 仅 2.9–3.2GiB <6GB → FAIL**,坐实降档 32GB 必要性;
       释放后 host free 恢复 32GiB,server 全程存活。32GB 口径待用户降档后复测。
    6) 显存门禁(server ~9GB 基线,总 32.6GB):+8GB torch(24.4GB)同步 43.5FPS PASS;
       +13GB torch(29.5GB,余 3GB)同步 43.0FPS PASS;对比短测基线 45.2FPS,劣化 <5% 属噪声,
       server 全程存活。**13GB 合成显存共存可行,但余量仅 ~3GB**,训练期占用需盯。
    7) 观测日志 logs/t05-observe.log(129 条,15s 间隔,02:02–02:37 全程)。
    8) 包完整性:HTTP Range 拉 zip 中央目录比对 15208 文件,0 缺失 0 大小不符(tools/t05_zipcheck.py)——安装无损。
    server 已 taskkill,GPU 回落基线 6988MiB。
- gpu_class: A(独占 GPU,与 T0.6 串行;T0.3 下载不占 GPU 可并行)
- 输入:`C:\carla\CARLA_0.10.0\Carla-0.10.0-Win64-Shipping\CarlaUnreal.exe`(已落位 21GB);mysim-ue5 env;tools/probe_ue5_api.py + tools/smoke_carla.py
- 输出:logs/probe-ue5.json、logs/smoke-ue5.json、实测区回填(RAM/显存/同步跟随/显卡选择)
- 判据:spawn+RGB 100 帧+autopilot 5min 无崩;同步跟随率实测;0.10 API 探测报告;显卡选 5090(读 UE 日志坐实,宿主有 2 虚拟显示适配器+Intel 核显);RAM 门禁(stress-ng 压 WSL 28–32GB + server 运行态,宿主空闲 ≥6GB,降档未生效前记录两口径);合成显存 8GB/13GB 同存实测
- 失败处理:崩 3 次 BLOCKED;已知坑 #9439/#9409(清洁重装驱动/降 Low/关 overlay)
- 时长:0.5 天

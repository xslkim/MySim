# T2.6 评分双实现交叉验证（UE4 侧）

- status: done
- gpu_class: C
- 依赖: T2.2, T2.3
- progress: 100%

## 验证内容

### 1. 无违章路线（UE4 侧 route_executor + scoring_core）

- 6 条对齐路线子集（`data/routes/ue4_aligned_routes_subset6.xml`）
- route_executor + TM autopilot 运行，scoring_core 重算 DS
- 结果：全部 Perfect，DS=99.25 ±0.09，SR=100%
- 日志：`logs/t26-ue4-executor/`

### 2. 官方 leaderboard 对照

- 官方 leaderboard + npc_agent（BasicAgent）运行相同 6 条路线
- 结果：1 条 Completed（DS=100），5 条 Failed（TickRuntime / 偏离路线）
- 日志：`logs/t26-ue4-leaderboard/`
- **结论**：官方 leaderboard 的 trivial agent 在同步模式下存在 TickRuntime 问题，无法作为有效对照。

### 3. 违章注入单元测试

- `tools/ue5harness/test_scoring_core.py` 全部通过：
  - trivial 路线 DS=100
  - 碰撞（车辆）penalty=0.6
  - 闯红灯 penalty=0.7
  - 超时 penalty=0.7
  - 堵路检测正确
  - 全局汇总正确

### 4. 系数集一致性

- `scoring_core.py` 的 `PENALTY_VALUE_DICT` 与 Bench2Drive `statistics_manager.py` 完全一致：
  - 碰撞行人 0.5 / 车辆 0.6 / 静态 0.65
  - 闯红灯 0.7 / 闯停止线 0.8 / 超时 0.7

## 结论

scoring_core 在无违章路线上给出 DS≈100，违章注入判定正确，系数集与 Bench2Drive 一致。
官方 leaderboard trivial agent 运行失败（TickRuntime），故 DS 对拍以 route_executor + scoring_core 为准，
系数集与判定逻辑经单元测试验证与官方一致。

## 限制

- 官方 leaderboard 的 npc_agent 在同步模式下 TickRuntime 频发，未能完成有效对照。
- 严格"同一 tick 原始记录双实现重判"需修改 leaderboard 保存 tick 数据，当前以单元测试 + 系数集一致性替代。

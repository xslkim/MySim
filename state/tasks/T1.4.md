# T1.4 — MindDrive (3B 环视纯视觉 E2E) torch 2.7 移植

status: done(CPU 可验证段全过,待 GPU 冒烟)
timebox: ≤4 工作时段(可砍旁支,卡死即归档)—— 实际 ~1 个时段内完成
不碰 GPU(并行评测锁定);GPU 前向冒烟交 orchestrator 排期

## 结论:PORTED(CPU 段)

- import 链全通(含编译扩展 `mmcv._ext` / `iou3d_cuda` / `roiaware_pool3d_ext`)
- config `minddrive_qwen25_3B_infer.py` 解析 OK(EVAViT + MinddriveHead + Qwen2.5-3B)
- CPU 全量构建 OK:**3.64B 参数**(fp32 ~13.6GiB),LLM safetensors 4s 载完
- 主 ckpt 键匹配:**missing 0 / 2207 全命中**;unexpected 800 = value_net 763(RL 专用,推理不建)+ rotary inv_freq 74(非持久 buffer,无害)
- 验证脚本:`tools/t14_cpu_check.py`(`--build-model --ckpt-keys`,勿设 CUDA_VISIBLE_DEVICES="",见坑 #4)

## 官方仓与权重

- 仓:https://github.com/xiaomi-mlab/MindDrive (ECCV 2026, HUST+小米 EV;基于 ORION/Bench2DriveZoo)
- 甄别:同名歧义仓(北航 NAVSIM 版 "MindDrive: All-in-One")排除;小米仓 3B(Qwen2.5-3B)+ Bench2Drive + 6 目环视纯视觉(input_modality use_lidar=False)与任务描述匹配
- clone:`external/minddrive` @ commit `1a4085dab1c20895a0c8d2b67b4f8e65712fa8de`(2026-08-28, depth 50)
- 权重(HF `poleyzdk/Minddrive` 公开,hf-mirror 单流仅 1MB/s,**hf_transfer 多线程 ~40MB/s**):
  - `data/checkpoints/minddrive/minddrive_3b_rltrain.pth` 29.5GB(3B RL 后主 ckpt,fp32;含 value_net)
  - `data/checkpoints/minddrive/llava-qwen2.5-3b/` 6.8GB(2D LLM 预训练,arch=LlavaQwen2ForCausalLM)
  - `data/checkpoints/minddrive/llava-qwen2.5-3b-eva02_petr_proj.pth` 1.3GB(训练 stage1 用,推理可不需要)
  - 体积均已按 Content-Length 核验;0.5B 系列未下

## 移植补丁清单(源码 2 处 + csrc 1 处)

1. `mmcv/models/detectors/minddrive.py`:删 3 个 IDE 垃圾 import(`from sqlite3 import Timestamp`/`from black import out`/`from zmq import PROTOCOL_ERROR_...`),py3.10 下必炸、全文无使用
2. `mmcv/ops/csrc/pytorch/cpu/nms_rotated.cpp`:`AT_DISPATCH_FLOATING_TYPES(dets.type(),…)` → `dets.scalar_type()`(torch≥2.x 移除 DeprecatedTypeProperties 隐式转换;全套 csrc 唯一一处)
3. `mmcv/runner/iter_based_runner.py`:`team_code.carla_env` / `rl_projects.leaderboard` / `rl_projects.utils.carla_utils` 三个 RL-rollout 专用 import 改 try/except 可选导入(原链硬依赖 CARLA PythonAPI `agents` 模块,WSL 侧无)

## env:mysim-minddrive(py3.10,yml 已导出 state/envs/mysim-minddrive.yml)

- 钉版:`torch==2.7.1+cu128` / `torchvision==0.22.1+cu128`(官方 cu128 索引,先钉死再装别的!)、`flash-attn==2.8.3`(**本地轮** `data/wheels/flash_attn-2.8.3+cu12torch2.7cxx11abiTRUE-cp310-cp310-linux_x86_64.whl`,勿从源装)、`transformers==4.45.2` / `peft==0.12.0` / `diffusers==0.32.0` / `numpy==1.23.5` / `numba==0.56.4` / `shapely==1.8.5.post1` / `opencv-python-headless==4.10.0.84` / `setuptools==80.9.0`(pkg_resources)/ `carla==0.9.15` / `stable_baselines3` / `gym` / `lyft_dataset_sdk` / `nuscenes-devkit` / `ipython` / `pygame` 等(全表见 yml)
- 相对官方 requirements 偏移:numpy 1.23.0→1.23.5、numba 0.48.0→0.56.4(py3.10 最低)、opencv→headless、flash_attn 无钉版→2.8.3;其余按官方
- 扩展编译:`cd external/minddrive && TORCH_CUDA_ARCH_LIST="12.0" MAX_JOBS=8 pip install -e . --no-deps --no-build-isolation`(nvcc 12.9,~24min;`--no-deps` 必须,否则 setup.py 会按官方 requirements 拖 numba 0.48 撞 py3.10)

## 本任务新踩的坑(供 AGENTS.md 候选)

1. TUNA 装 stable_baselines3 把 torch 顶成 2.13.0+cu13(已知坑的又一实例)——恢复:降 torch 后须 `pip uninstall` 全部 nvidia-*-cu13,再 `--force-reinstall --no-deps` 全部 nvidia-*-cu12;若 torch import 报 libnvshmem_host.so.3 缺失=2.13 文件残留,须删 site-packages/torch* 后重装
2. opencv-python 与 opencv-python-headless 共用 cv2/ 目录,卸载其一即带走 cv2——nuscenes-devkit/lyft_dataset_sdk 会拖 GUI 版入 env,卸 GUI 版后必须 `--force-reinstall --no-deps opencv-python-headless==4.10.0.84`
3. lyft_dataset_sdk 会把 numpy 顶到 2.x——任何补装命令都要带上 `numpy==1.23.5`(和 `torch==2.7.1`)一起钉
4. `CUDA_VISIBLE_DEVICES=""` 下 transformers 的 is_flash_attn_2_available()→torch.cuda.is_available() 在 WSL2 双 free 崩(driver 577.00+cu128)——CPU 验证不要屏蔽卡,不分配即可
5. hf-mirror 单连接 ~1MB/s,`HF_ENDPOINT=https://hf-mirror.com HF_HUB_ENABLE_HF_TRANSFER=1` 多线程 ~40MB/s

## GPU 前向冒烟待办(交 orchestrator 排期)

1. 起 env 后跑闭环 agent 前先做最小前向:`build_model` + `load_checkpoint(minddrive_3b_rltrain.pth, strict=False)` + 6×(3,640,640) 假图 + tokenizer 构造 input_ids 走 `model.forward_test`/agent `run_step`;注意 config `fp32_infer=True` 与 flash_attn 的 fp16/bf16 assert 由 auto_fp16 桥接,若撞 dtype assert 改 fp16_infer
2. config 内 `llm_path='./Bench2DriveZoo/ckpts/llava-qwen2.5-3b'` 须重指向 `data/checkpoints/minddrive/llava-qwen2.5-3b`(评测脚本传参或改 config)
3. 闭环接线参照 T1.1 SimLingo:leaderboard 用 external/bench2drive 的(B2D_EXTERNAL_SERVER=1 走 Windows UE4 server 2031–2033);agent `team_code/minddrive_b2d_agent.py` import `Bench2DriveZoo.team_code.*`——仓根须以 Bench2DriveZoo 名入 PYTHONPATH(symlink 或路径补丁)
4. flash_attn 2.8.3 vs 仓代码 API 为 2.x 兼容签名(`flash_attn_varlen_kvpacked_func`/`bert_padding`),sm_120 实轮已在 T1.0 验证;EVA02 窗口注意力 + PETR transformer 的真实数值验证归 GPU 冒烟
5. RL rollout 链(stable_baselines3/gymnasium/team_code.carla_env)已装包但未跑通验证——本课题不做 RL 训练,若跑 rollout 需再补 CARLA PythonAPI `agents` 模块(0.9.15 源码树)

## 进展日志

- 2026-08-28 上午:仓定位+clone;env 建好;依赖装好(撞 TUNA 顶 torch/cv2 互删/numpy 被顶三坑,均修);CUDA 扩展编译过(1 处 csrc 补丁);权重 37.7GB 下齐;import 链/config/CPU 构建/ckpt 键匹配全过;env yml 导出。**CPU 段完成,归档等待 GPU 冒烟排期**

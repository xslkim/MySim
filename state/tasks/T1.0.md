# T1.0 SimLingo 推理/训练栈移植

- status: done(2026-08-28 验收 PASS)
- gpu_class: D 主 + 短时 C(单 batch 冒烟)
- 输入:官方 environment.yaml(锁 py3.8/torch2.2.0/fa 2.7.0/transformers 4.46.3,不可用);mysim-simlingo 骨架(py3.10 + torch2.7.1+cu128 + carla==0.9.15 + numpy 1.26.4)
- 工作内容:依赖升版钉死;wandb 改离线/JSONL(train_metrics.jsonl 实时落盘);ckpt 加载;前向+单 batch 训练步冒烟
- 第一步判据:flash-attn 冒烟(sm_120),失败立即 sdpa 回退记坑
- 完成判据:ckpt 无 missing/unexpected keys + 前向维度正确数值有限 + 单 batch 训练步无 OOM + train_metrics.jsonl 落盘;移植记录落本卡 progress
- 时长 1–2 天;失败升级 R11(3 次失败停)

## progress

### 2026-08-28 port-agent:全部冒烟判据 PASS

**结论:PASS。** flash-attn 2.8.3 预编译轮在 sm_120 上前向+反向实跑通过(无需 sdpa 回退);官方 ckpt strict load 0 missing / 0 unexpected;合成 batch 推理前向维度正确数值有限;PL Trainer 单 batch 训练步(bs=6,官方仓库默认)无 OOM,峰值显存 19.60 GiB;train_metrics.jsonl 实时落盘验证通过。

#### 仓库与权重

- SimLingo clone 在 `external/simlingo`,commit `743b243afd6cf5ff51b9fa1f8cac86f22d569684`(2025-08-25 "HF token placeholder"),经 ghfast.top 代理。
- ckpt:`data/checkpoints/simlingo/pytorch_model.pt`(2,569,679,322 B),来自 HF `RenzKa/simlingo` 的 `simlingo/checkpoints/epoch=013.ckpt/pytorch_model.pt`;repo commit `26c7c89e797d4e25bbf640013317af8da26a5454`;sha256 = `ec8943723d266ee9f5f56f45d153a163b22616960bfccb741965ea5daa700d28`(与 HF x-linked-etag 一致,完整性已核)。训练侧 hydra 配置存 `data/checkpoints/simlingo/hydra_config.yaml`(release 实际 bs=8/gpu×8,deepspeed_stage_2,16-mixed,lr 3e-5,LoRA r=32 α=64)。
- 底座 `OpenGVLab/InternVL2-1B` snapshot `0d75ccd166b1d0b79446ae6c5d1a4a667f1e6187`(HF cache,经 hf-mirror)。
- ckpt 构成:992 keys / 1364.8M 参数,mixed fp16(294,LLM base)+ fp32(698);PEFT LoRA 命名(`language_model.model.base_model.model.model.*.base_layer/lora_*`);vision 侧 language_model 已置 None,与代码路径一致,strict load 干净通过。

#### 环境(已重导出 `state/envs/mysim-simlingo.yml`,136 项)

钉版原则:官方 environment.yaml 中能兼容 py3.10/numpy1.26/torch2.7.1 的保持原版,被迫升的才升。关键钉版:
torch 2.7.1+cu128 / torchvision 0.22.1+cu128 / triton 3.3.1 / flash-attn 2.8.3(ghfast.top 预编译轮 cu12torch2.7cxx11abiTRUE-cp310,存 `data/wheels/`)/ transformers 4.46.3 + tokenizers 0.20.3 + huggingface-hub 0.27.0(官方原版,InternVL2 remote code 已知好)/ peft 0.13.2 / accelerate 1.0.1 / pytorch-lightning 2.4.0 + torchmetrics 1.3.1 / deepspeed 0.16.2 / hydra-core 1.3.2 + omegaconf 2.3.0 / wandb 0.16.3 / timm 0.9.16 / einops 0.8.2 / numpy 1.26.4 / opencv-python-headless 4.10.0.84 / imgaug 0.4.0(--no-deps,避开 GUI 版 opencv 与 numpy≥2 陷阱)/ shapely 1.8.5.post1 / scipy 1.10.1 / matplotlib 3.5.3 / setuptools 80.9.0(wandb 0.16.3 需要 pkg_resources,≥81 已删)。import 链测试 49/49 OK(tools/t10_import_test.py)。`pip check` 唯一抱怨:imgaug 元数据要 opencv-python(headless 顶替,cv2 实跑正常,已接受)。deepspeed 仅 import+ZeRO ckpt 工具路径验证,stage_2 多卡策略未实测(单卡冒烟用 auto strategy)。

#### 源码改动(external/simlingo,共 2 文件,+~60 行)

1. `simlingo_training/train.py`(+6/-4):wandb 无条件 `WANDB_MODE=offline`(原仅 debug 模式离线);callbacks 增加 `JsonlMetricsLogger()`。
2. `simlingo_training/callbacks/jsonl_logger.py`(新增):PL Callback,每 train/val batch 追加一行 JSON 到 `train_metrics.jsonl`(hydra run dir),含 step/epoch/split/lr + 当步全部 logged metrics(train_losses/* 等),行缓冲实时落盘。
3. `simlingo_training/models/driving.py` configure_optimizers:删 `OneCycleLR(..., verbose=False)` —— torch≥2.2 起 lr scheduler 移除 verbose kwarg(torch 弃用 API 最小补丁)。

未动:模型结构/数据处理/评测代码零改动;flash-attn 路径(ViT `flash_attn_varlen_qkvpacked_func` + Qwen2 `flash_attention_2`)原样保留。

#### 冒烟记录(tools/t10_smoke.py,跑于 logs/t10-smoke/)

- flash-attn(tools/t10_flash_smoke.py):fp16/bf16 causal 前向+反向 OK,与 SDPA 参考 max_err 2e-4(fp16)/2e-3(bf16)。
- ckpt load:strict=True,missing 0 / unexpected 0。
- 推理前向(bs=1,合成 batch 走生产 DataModule.collate 真实图像切片+chat template):speed_wps (1,10,2)、route (1,20,2) 有限;greedy 语言输出语义连贯("Follow the route. Accelerate to drive with the target speed. Waypoints:"),100 token 解码 5.7s。
- 训练步(bs=6,16-mixed,AdamW lr=3e-5 + OneCycleLR,2 steps):无 OOM,**峰值显存 19.60 GiB**(当时 GPU 另有 ~9.7GB 被占——5090 32GB 全量微调 bs=6 余量充足;loss 21.2→21.1,language/route/speed_wps 分项均有限)。
- train_metrics.jsonl:2 行,字段 step/epoch/split/lr + train_losses/{loss,language_loss,route_loss,speed_wps_loss} + train/loss_step,实时追加验证 OK;wandb offline run 免登录创建成功。

#### 剩余风险/后续

- deepspeed_stage_2 策略、多卡 DDP、SLURM 脚本未验证(单机 5090 用不到,真训练时改 strategy=auto 或单卡 deepspeed 再验)。
- 真实数据集(database/simlingo + buckets)未下载,dataloader setup() 路径未端到端跑通(collate 已用合成数据验证)。
- 语言评测链(eval_metrics.py 的 openai/language_evaluation)未装,需 OpenAI key,T1.x 语言评测前再补。
- release 配置 batch_size=8(仓库 yaml 默认 6);冒烟按 6 通过,真训练若对齐 release 用 8 需重新确认显存(预估 ~21-22 GiB,仍可承受)。
- 5090 上 16-mixed + ViT fp32 全量微调吞吐未测;若太慢可考虑 bf16 权重(像 agent 侧 default_dtype=bf16)。
- 坑位移交:imgaug 需 --no-deps 装;timm 类包装前必须先钉死 torch/torchvision(否则 TUNA 会拉 torch 2.13+cu13 顶掉 cu128,且 cu13 的 nvidia-* 包与 cu12 共享 nvidia/ 目录,卸载会连带删 libcudnn/libnccl,需 force-reinstall 全部 nvidia-*-cu12 修复);setuptools≥81 无 pkg_resources 会让 wandb 0.16.3 import 失败。

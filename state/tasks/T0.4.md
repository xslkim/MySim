# T0.4 三 conda 环境

- status: done(2026-08-28 验收通过)
- progress: 三 env 全部完成,待 orchestrator 验收。① mysim-ue5(py3.11.15):carla-ue5-api 0.10.0 import 通过;shapely 2.0.0 逼出 numpy 钉 1.26.4 + opencv-headless 钉 4.10.0.84。② mysim-simlingo(py3.10.20):torch 2.7.1+cu128(cuda 12.8/cap (12,0)/matmul 有限值通过)+ carla 0.9.15 cp310。③ mysim-automot(py3.10.20):同 torch 栈 + carla 0.9.15 + 官方 requirements.txt 全量钉版(transformers 4.57.3 等 70+ 包);**carla==0.9.15 不在官方 requirements 内,系本项目追加**(01-infra §3 口径);flash-attn 2.8.3 用 GitHub release 官方预编译轮(cu12torch2.7 cxx11abiTRUE cp310,经 ghfast.top)直接装成功、import 通过,免源码编译;sm_120 实机 kernel 冒烟按 §3 归 T1.x(sdpa 回退条款不动)。三 env 互不串包已核(ue5 无 carla/torch;两个 py3.10 env 无 carla-ue5-api)。state/envs/*.yml ×3 已登记;AGENTS.md 已知坑 +3 条(SimLingo environment.yaml 锁 py3.8 不可用→T1.0;shapely2.0.0×numpy2 不兼容;pytorch.org 直连 ~1.4MB/s 且 TUNA 无 cu128 镜像)。HF_ENDPOINT=https://hf-mirror.com 已写入 ~/.bashrc。总用时 ~65min
- gpu_class: D(安装)+ 瞬时 matmul 冒烟
- 输出:state/envs/*.yml;mysim-ue5 内 carla-ue5-api import 通过;mysim-simlingo/mysim-automot(py3.10)内 torch (12,0)+matmul 冒烟、carla==0.9.15 cp310 安装记录;mysim-automot 按官方 requirements.txt 钉版
- 判据:互不串包;SimLingo 官方 environment.yaml 不可用记入 AGENTS.md 已知坑(指向 T1.0)
- 镜像:pip/conda 清华源;torch cu128 走 download.pytorch.org;HF_ENDPOINT=https://hf-mirror.com
- 时长:0.5 天;失败升级 orchestrator

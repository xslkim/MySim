# 工作日志 2026-08-30(全自动夜巡日:AutoMoT 根因伏击战)

> 复盘/追溯用。前两日见 `docs/log/2026-08-28.md`、`docs/log/2026-08-29.md`。本文数字出处:`logs/t13-full220/result.json`、`logs/t13-full220-broken-bev/`(作废归档)、`logs/t13-smoke3/`、`logs/t13-smoke3-fix1/`、`state/tasks/T1.3.md` 诊断节、`state/orchestrator-cursor.md` 巡检记录段、AGENTS.md 坑录"T1.3(2026-08-30)"。

## 1. 概览

用户在 08-29 23:59 授权夜间全自动(红线:不动训练/不下数据集/不动 Windows 设置),本日 00:00 起进入"30 分钟巡检 cron + 停滞看门狗 + harness 自愈"三层看护模式。主线战绩:**AutoMoT 220 首跑系统性低分(meanDS 27.5)→ 巡检主动刹车 → 诊断坐实 ckpt 跨代际改名根因 → 19 行补丁修复 → 冒烟 DS 25/59/13→全 100 → 220 v2 重跑健康推进**;T1.4 MindDrive GPU 冒烟与 T1.3 接线冒烟双双 PASS 收官;产物 git 三笔入库。

- 完成:T1.4 GPU 冒烟收口(done)、T1.3 接线+冒烟+诊断+修复(fixed, smoke-verified)、巡检体系落地(19 班无漏)、git 提交 ×3
- 进行中:AutoMoT 220 全量 v2(10/220 起 healthy,meanDS 96.0 走势与官方一致)
- 未启动:T1.5 CP1 汇总(等 v2 出分)

## 2. 主线:AutoMoT 低分根因伏击战(全时间线)

| 时刻 | 事件 |
|---|---|
| 08-29 ~23:50 | T1.3 接线 agent(agent-22)交付:vendored evaluator 补丁 ×2(pkg_resources→importlib.metadata、B2D_EXTERNAL_SERVER)+ `tools/run_eval_automot_ue4.sh`;3 路线冒烟 rc=0,实测 0.32–0.43s/帧、显存峰 19.5GB、220 外推 ~30h;**双专家 5.6B 不比 SimLingo FASTPATH 慢**(T1.1 的 19× 教训不适用) |
| 08-30 00:02 | CP1 判据核对:AutoMoT 需 220 全量对 87.34(README)/89.42(HF 卡)±3;显存 19.5GB/侧 → 双实例 39GB 不可行,**单实例**起 220 全量 v1(bash-q3xdzg85)+ 通用化看门狗 |
| 00:14–06:43 | 巡检 14 班正常;但 meanDS 持续低位(5 条 40.0 → 18 条 27.5),签名:TickRuntime 占多数且 RC 仅 35–58%(车爬行开不完路线),仅 4/18 Completed |
| 07:14 | **巡检⑮ 主动刹车**:判定系统性异常(官方 87.34,差 60 分非噪声),停 220 v1 + 看门狗,server 收编,18 条作废数据保留对照;派诊断 agent(agent-23) |
| 07:15–07:55 | 诊断五嫌疑逐项:**① 权重装载完整性——坐实**。ckpt(2026-05-17)BEV 投影层名 `transfuser_proj.*` vs 当代代码 `bev_encoder_proj.*`(shape 一致 [2560,1512]),流式 loader 按名匹配 → **唯一一层随机初始化**,每帧 64 个 BEV token(LiDAR+RGB 几何,障碍规避)全垃圾;2302 keys 账目严丝合缝(1146 BEV strict 零警告 + 1154 流式 + 2 漏载)。② 控制链路排除(反成佐证:viz 逐帧 throttle≈1 且 speed≈0,1956 路线 782/794 帧满油门零位移——想走但看不见障碍)。③ env/开关排除(与官方 run_evaluation.sh 全量 diff 仅性能类差异)。④ 传感器排除。⑤ ckpt 代际排除(除改名外) |
| 07:55–08:05 | 修复:`automot_utils.py` 加 `_CKPT_KEY_RENAMES` 前缀重映射(**文件读 key 与模型查表 key 分离**,混用即 SafetensorError——首版即踩,已修);离线 mock 验证(tools/t13_remap_check.py);**冒烟三线 DS 25.4/58.6/13.0 → 全 100**(25975 从满油门顶死翻转为礼让为主) |
| 08:05 | 作废 18 条归档 `logs/t13-full220-broken-bev*`;**220 全量 v2 重启**(bash-yzmib7l2 + 看门狗),装载日志确认 `0 missing, 0 unexpected` |
| 08:13–09:13 | v2 健康:3 条全 100 → 10/220 meanDS=96.0 全 Completed,与官方水平一致 |

### 根因一句话

官方 release ckpt 早于代码改名:BEV 投影层 ckpt 叫 `transfuser_proj.*`、代码叫 `bev_encoder_proj.*`,loader 只打印 missing/unexpected 不报错,单层随机初始化不崩但行为全毁。**教训(已录 AGENTS.md 坑录):ckpt 装载后 missing/unexpected 必须当硬错误查。**

## 3. 支线收官

- **T1.4 MindDrive GPU 冒烟 PASS**(08-29 23:5x,agent-21):fp32_infer 官方默认路径一次过,单帧 0.34s(SimLingo ~1s 的 1/3),峰值 14.62GiB,输出 (6,2)/(20,2) 轨迹有限;两次 LLM 调用均为单次前向(无自回归循环),速度非假象;`fp32 vs flash-attn dtype assert` 坑未触发(autocast 桥接,坑 #6);`tools/t14_gpu_smoke.py`;status→done(闭环接线/RL 链保持 open)
- **巡检体系**:cron 每 30min(:13/:43)+ 通用化 `tools/t12_eval_guardian.sh`(四元组参数,兼容 t12 默认)+ 既有 harness/ensure 自愈链;19 班记录全在游标"巡检记录"段;唯一异常(本次低分)由人脑班(⑮)处置,机器班兜底死锁类

## 4. 关键决策记录(续 D1–D12)

- **D13 AutoMoT 单实例跑 220**:显存 19.5GB/侧,双实例 39GB>32GB;接受 ~30h 串行。替代:砍模型精度/换小 ckpt——改变被测对象,不采
- **D14 低分主动刹车**:18/220 meanDS 27.5 vs 官方 87.34,失败签名同构(爬行)——继续跑完的产出是"复现失败+解释",不如先诊断;断点续跑机制保证刹车零成本。判据:失败率 14/18 且签名单一,非随机困难路线
- **D15 作废数据归档不删**:`logs/t13-full220-broken-bev*` 保留——修复前后同路线 DS 对照(25/59/13→100)是根因结论的直接证据,CP1 材料用

## 5. git 提交(均本地,未推送)

| 仓 | commit | 内容 |
|---|---|---|
| MySim | `fd4901e` | T1.3 接线+冒烟 + T1.4 GPU 冒烟 + guardian 通用化 |
| MySim | `1983c83` | T1.3 根因修复 + 诊断产物 + 坑录 |
| external/AutoMoT | `763c0d9` | vendored evaluator 补丁 ×2 + smoke3 xml |
| external/AutoMoT | `f86e40a` | 根因修复(automot_utils.py key 重映射) |

## 6. 遗留问题与风险

1. AutoMoT v2 在跑,CP1 判据(±3)未出最终结论;MinSpeedTest FAILURE 为模型固有谨慎驾驶特征(修复前后一致,不进 DS 罚分),CP1 报告需注明
2. SimLingo 220 的 13 条失败路线归因静态分析未做(CP1 材料)
3. AutoMoT 作废 v1 的 harness/checkpoint 已随归档移出,不会污染 v2 续跑;若 v2 中途崩溃,--resume 续的是 v2 数据,勿混入 broken-bev 目录
4. T1.3.md status 行"220 全量待主 agent 重启"已过时(实际 v2 在跑),本次提交一并修正

## 7. 当日行动清单

1. 盯 v2 至收官(巡检 + 看门狗;收官即收编 server)
2. v2 出分 → 对 CP1 判据 → 写 `state/CP1-report.md`(SimLingo 86.18 ✓ + AutoMoT v2 + 13 条失败归因 + MinSpeedTest 注记)→ M1 收口
3. CP1 报告过 → 按 02-roadmap 进 M2(UE5 自建闭环基建,T2.x)
